# Various locations, can be overridden in a local file, user.properties
project.home=D:/softwares/continuum-1.0.3/apps/continuum/working-directory/19/domain
xradar.home=D:/softwares/xradar-base-0.99.1

# Prefix for each release directory and release specific property file.
# Version number is appended (see version.list below).
# I.e. in this case, release directories are named release1, release2, ...
# and release specific property files are named etc/release1.properties, etc/release2.properties, ...
version.prefix=version

# Current version -must- be the last version in the version list.
version.current=1.0
version.list=1.0
